import type { DerivativeFunc } from '../../../../StyleContext';
import type { MapToken, SeedToken } from '../../interface';
declare const derivative: DerivativeFunc<SeedToken, MapToken>;
export default derivative;
